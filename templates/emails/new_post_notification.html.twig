<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 font-sans">
    <div class="max-w-2xl mx-auto my-8 px-4">
        <!-- Header -->
        <div class="bg-gradient-to-r from-indigo-600 to-purple-600 rounded-t-lg p-8 text-center">
            <h1 class="text-3xl font-bold text-white">📢 Nouveau Post sur MiniRSN</h1>
        </div>

        <!-- Content -->
        <div class="bg-white border-x border-gray-200 p-8">
            <p class="text-gray-700 text-lg mb-6">
                Un nouveau post vient d'être publié sur MiniRSN !
            </p>

            <!-- Post Info Card -->
            <div class="bg-gradient-to-br from-indigo-50 to-purple-50 rounded-lg p-6 border border-indigo-200 shadow-sm">
                <!-- Post Title -->
                <h2 class="text-2xl font-bold text-indigo-700 mb-4">
                    Nouveau post de {{ author.name ?? author.email }}
                </h2>

                <!-- Post Content -->
                <div class="text-gray-700 leading-relaxed mb-4">
                    {{ post.content|length > 200 ? post.content|slice(0, 200) ~ '...' : post.content }}
                </div>

                <!-- Image Badge -->
                {% if post.image %}
                <div class="inline-flex items-center gap-2 bg-indigo-100 text-indigo-700 px-3 py-1 rounded-full text-sm font-medium mb-4">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                    Ce post contient une image
                </div>
                {% endif %}

                <!-- Author Info -->
                <div class="border-t border-indigo-200 pt-4 mt-4">
                    <div class="flex items-start gap-4">
                        <div class="flex-shrink-0">
                            <div class="w-12 h-12 bg-indigo-600 rounded-full flex items-center justify-center text-white font-bold text-lg">
                                {{ (author.name ?? author.email)|slice(0, 1)|upper }}
                            </div>
                        </div>
                        <div class="text-sm text-gray-600 space-y-1">
                            {% if author.name %}
                            <p class="font-semibold text-gray-800">
                                👤 {{ author.name }}
                            </p>
                            {% endif %}
                            <p>
                                ✉️ {{ author.email }}
                            </p>
                            <p>
                                🕐 {{ post.createdAt|date('d/m/Y à H:i') }}
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- CTA Button -->
            <div class="text-center mt-8">
                <a href="{{ url('post_show', {'id': post.id}) }}"
                   class="inline-block bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white font-semibold py-3 px-8 rounded-lg shadow-lg transition-all duration-200 transform hover:scale-105">
                    Voir le post →
                </a>
            </div>
        </div>

        <!-- Footer -->
        <div class="bg-gray-100 rounded-b-lg border border-gray-200 border-t-0 p-6 text-center">
            <p class="text-gray-600 text-sm mb-2">
                Ceci est une notification automatique de MiniRSN
            </p>
            <p class="text-gray-500 text-xs">
                Vous recevez cet email car vous êtes membre de la plateforme
            </p>
        </div>
    </div>
</body>
</html>
