// Configuration Tailwind CSS pour Mini RSN
tailwind.config = {
  darkMode: 'class', // Active le dark mode via la classe 'dark' sur <html>
  theme: {
    extend: {
      // Vous pouvez ajouter des personnalisations ici plus tard
    }
  }
}
